# Environment and secrets
.env
.gitconfig.local
git/.config/git/work.gitconfig
git/.config/git/personal.gitconfig

# IDE and editor
.vscode

# AI agent context (local only)
.claude
.claudeignore
CLAUDE.md

# macOS
.DS_Store

# Analysis and reports
analysis
*audit-report*

# Override global gitignore - we WANT to track .config in this repo
!/.config
!/.config/**

# Neovim
*.swp
*.swo
*~
.netrwhist
.nvim-state/
.local_state/
lazy-lock.json

# SSH keys (config is tracked, keys are NOT)
ssh/.ssh/id_*
ssh/.ssh/known_hosts
ssh/.ssh/authorized_keys

# Session Artifacts
dotfiles_analysis_report.md
commits-backup-*.txt
